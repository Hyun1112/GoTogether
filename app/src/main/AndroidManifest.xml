<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="같이 가GO"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp">

        <activity android:name=".MainActivity"></activity>
        <activity android:name=".JoinActivity"></activity>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SeoulActivity"></activity>
        <activity android:name=".EtcActivity"></activity>
        <activity android:name=".AliceActivity"></activity>
        <activity android:name=".MoneActivity"></activity>
        <activity android:name=".ForestActivity"></activity>
        <activity android:name=".BanksyActivity"></activity>
        <activity android:name=".YumiActivity"></activity>
        <activity android:name=".YosigoActivity"></activity>
    </application>
    
    <uses-permission android:name="android.permission.CALL_PHONE"/>

</manifest>